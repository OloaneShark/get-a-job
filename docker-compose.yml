
services:
  web:
    build: .

    ports:
      - "5000:5000"

    env_file:
      - .env

    volumes:
      - .:/app

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

    restart: unless-stopped
  